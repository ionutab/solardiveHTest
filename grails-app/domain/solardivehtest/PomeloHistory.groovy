package solardivehtest

class PomeloHistory implements TangerineHistory{

    Pomelo pomelo

    DefaultHistory history = new DefaultHistory()
    static embedded = ['history']

    static constraints = {
    }
}
